<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>testtube - <%= @page_title || "Estimation tool for big kids" %></title>
<%= javascript_include_tag "application" %>
<%= stylesheet_link_tag    "application", :media => "all" %>
    <%# <%= javascript_include_tag("jquery-1.9.1")%>
    <%= javascript_include_tag("TT")%>
    <%= stylesheet_link_tag("testtube")%>
</head>
<body>
  <div id="wrapper">
    <header class="page-header">
      <div class="page-header-contents">
        <div class="page-header-headings<%= " project" if defined?(@project) %>">
          <% if defined?(@project) %>
            <p><%= @project.name %> (<%= @project.description %>)</p>
          <% end %>
        </div>
        <%= render "shared/tt_app_menu"  %>
        <menu class="tube-menu">
          <ul>
            <li>
            <%= link_to image_tag("tub-black-home-2.png"), \
              welcome_index_path, \
              :class => 'home' %>
            </li>
            <li>
            <%= link_to image_tag("tubes-red-projects-2.png"), \
              projects_path, \
              :class => 'projects' %>
            </li>
          </ul>
        </menu>
      </div>
      <a href="/projects">
        <%= image_tag "logo-7.png", :class => "page-logo" %>
      </a>
    </header>
    <div class="main-body-section">
      <% flash.each do |key, value| %>
        <p><%= value %></p>
      <% end %>
      <%= yield %>
    </div>
 </div>
 <div id="mask" class="TTpopup-background-mask"></div>
</body>
</html>

