      <table class="form-fields">
        <tr>
          <td>Name:</td>
          <td><%= @project.name %></td>
        </tr>
        <tr>
          <td>Description:</td>
          <td><%= @project.description %></td>
        </tr>
        <tr>
          <td>Is public?</td>
          <td>
            <%= @project.is_public ? "Public (anyone who finds it, sees it)" : "Private (need to be a member to see it)" %>
          </td>
        </tr>
      </table>
